return {
	ip_address = "SERVER_IP_HERE",
	port = 5000,
	secret = "", -- SERVER_SECRET / roblox server credentials

	polling_delay = 1,
	heartbeat_delay = 15,
	timeout = 30,

	verbose_logging = true,
	verbose_errors = true,
}
